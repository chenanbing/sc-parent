spring:
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: sc-config


eureka:
  client:
    serviceUrl:
      defaultZone: http://cab:cab19891023@localhost:8761/eureka/
