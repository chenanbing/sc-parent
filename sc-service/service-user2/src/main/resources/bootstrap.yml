spring:
  cloud:
    config:
      profile: test
      label: master
      discovery:
        enabled: true
        service-id: sc-config


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
